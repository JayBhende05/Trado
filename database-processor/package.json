{
  "name": "database-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc -b",
    "start": "node build/server.js",
    "dev": "npm run build && node build/server.js",
    "seed:db": "npm run build && node build/seed-db.js",
    "refresh:views": "npm run build && node build/cron.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.22.0",
    "redis": "^6.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}